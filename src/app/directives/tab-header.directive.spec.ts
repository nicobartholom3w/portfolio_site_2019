import { TabHeaderDirective } from './tab-header.directive';

describe('TabHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new TabHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
